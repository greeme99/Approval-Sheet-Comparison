# PDF 승인원 버전 비교·변경사항 요약 Full Version 가이드

> 목적: 승인원/사양서 PDF의 구버전과 신버전을 비교해 표·본문의 변경사항을 재현 가능하고 검증 가능한 방식으로 추출한다.
> 핵심 변경: 1~3단계는 기존 흐름을 유지하고, 4단계는 LLM 요약 대신 구조적 DataFrame 매칭과 규칙 기반 요약 테이블 생성으로 대체한다.

## 0. 전체 아키텍처

```text
PDF 업로드
  ↓
1단계: 파일 검증·메타데이터 확인
  ↓
2단계: PDF 텍스트/페이지/표 추출
  ↓
3단계: 표 정규화·본문 텍스트 정리
  ↓
4단계: DataFrame outer join + 규칙 기반 diff
  ↓
검증·신뢰도 표시
  ↓
Streamlit st.dataframe() 렌더링 / CSV·JSON export
```

이 설계는 “추출”과 “비교·요약”을 분리한다. PDF 파서는 표를 DataFrame으로 만들고, 4단계는 그 DataFrame을 결정론적으로 비교한다. 따라서 동일한 입력은 동일한 결과를 내며, LLM의 환각이나 요약 누락을 줄일 수 있다.

## 1. 1단계 — 입력 검증

### 목표

- 구버전/신버전 파일을 각각 식별한다.
- PDF인지, 암호화되었는지, 페이지 수와 파일 크기가 정상인지 확인한다.
- 원본 해시를 저장해 결과와 입력을 연결한다.

```python
from pathlib import Path
import hashlib


def sha256_file(path: str) -> str:
    h = hashlib.sha256()
    with open(path, "rb") as f:
        for chunk in iter(lambda: f.read(1024 * 1024), b""):
            h.update(chunk)
    return h.hexdigest()


def validate_pdf(path: str) -> dict:
    p = Path(path)
    if not p.exists():
        raise FileNotFoundError(path)
    if p.suffix.lower() != ".pdf":
        raise ValueError("PDF 파일만 입력할 수 있습니다.")
    if p.stat().st_size == 0:
        raise ValueError("빈 파일입니다.")
    return {"path": str(p), "size": p.stat().st_size, "sha256": sha256_file(str(p))}
```

운영 환경에서는 업로드 파일명을 그대로 신뢰하지 말고 임시 저장소에 복사한다. 파일명, 해시, 업로드 시각, 처리 버전, 파서 설정을 결과 메타데이터에 함께 기록한다.

## 2. 2단계 — PDF 추출

### 2.1 텍스트 기반 표: pdfplumber

`pdfplumber`는 PDF 객체 좌표와 텍스트·표 추출을 함께 제공하며, 기계 생성 PDF에서 특히 적합하다. 표 감지는 선과 텍스트 정렬을 이용하고 `extract_table()`/`extract_tables()`를 제공한다. `table_settings`와 `crop()`으로 영역별 튜닝이 가능하다. 단, OCR 스캔 PDF의 표 추출에는 강하지 않으므로 OCR 경로를 별도로 둔다. [web:1]

```python
import pdfplumber


def extract_pdfplumber_tables(pdf_path: str) -> list[dict]:
    results = []
    with pdfplumber.open(pdf_path) as pdf:
        for page_no, page in enumerate(pdf.pages, start=1):
            tables = page.extract_tables()
            for table_no, raw in enumerate(tables, start=1):
                if raw:
                    results.append({
                        "page": page_no,
                        "table": table_no,
                        "rows": raw,
                    })
    return results
```

선이 없는 표는 다음과 같이 텍스트 전략을 시험한다.

```python
TEXT_TABLE_SETTINGS = {
    "vertical_strategy": "text",
    "horizontal_strategy": "text",
    "text_x_tolerance": 3,
    "text_y_tolerance": 3,
}
```

페이지 전체에서 표를 찾지 말고, 가능하면 표의 bounding box를 설정해 제목·각주가 행으로 섞이지 않도록 한다. `debug_tablefinder()`와 `PageImage`로 검출 선, 교차점, 셀을 시각 검증한다. [web:1]

### 2.2 대안 파서: pypdf_table_extraction / Camelot

사용자가 제시한 `pypdf_table_extraction`은 Camelot에서 파생된 표 추출 라이브러리로, DataFrame·CSV·JSON 등으로 내보내고 parsing report의 accuracy/whitespace 같은 지표를 제공했다. 그러나 원 저장소는 2025-04-11 아카이브되었고 README는 deprecated 및 `camelot-py` 사용을 안내하므로 신규 프로젝트에서는 유지보수 상태를 확인한 뒤 Camelot을 우선 검토한다. 또한 이 계열은 텍스트 기반 PDF 중심이며 스캔 문서에는 직접 적용하지 않는다. [web:10]

```python
# 기존 호환 코드가 있을 때만 사용
import pypdf_table_extraction

tables = pypdf_table_extraction.read_pdf("old.pdf", pages="all")
df = tables[0].df
report = tables[0].parsing_report
```

현재 유지보수 경로를 택한다면:

```python
import camelot

tables = camelot.read_pdf("old.pdf", pages="all", flavor="lattice")
# 격자선이 없는 표는 flavor="stream" 시험
df = tables[0].df
report = tables[0].parsing_report
```

파서 선택 기준:

| 문서 유형 | 우선 경로 | 보완 |
|---|---|---|
| 선이 뚜렷한 디지털 표 | Camelot lattice 또는 pdfplumber lines | crop, tolerance 조정 |
| 선이 없는 정렬형 표 | pdfplumber text 또는 Camelot stream | 열 좌표 검증 |
| 스캔/이미지 PDF | OCR + 표 구조 인식 | 수동 검수 필수 |
| 복잡한 표·다국어·레이아웃 | Marker/PDF-Extract-Kit 등 | 결과를 DataFrame으로 변환 |

## 3. 3단계 — 정규화

추출 결과는 바로 비교하지 않는다. 셀의 줄바꿈·공백·유니코드·결측치를 정규화하고, 헤더 행을 제거하며, 병합 셀에 대한 정책을 적용한다.

```python
import pandas as pd
import re
import unicodedata


def clean_cell(value) -> str:
    if pd.isna(value):
        return ""
    value = unicodedata.normalize("NFKC", str(value))
    value = value.replace("\\n", " ").replace("\n", " ")
    return re.sub(r"\\s+", " ", value).strip()


def normalize_two_col_table(raw_df: pd.DataFrame) -> pd.DataFrame:
    if raw_df.shape[1] < 2:
        raise ValueError("최소 2개 컬럼이 필요합니다.")
    df = raw_df.iloc[:, :2].copy()
    df.columns = ["항목", "사양"]
    df = df.map(clean_cell)
    df = df[(df["항목"] != "") & (df["항목"].str.lower() != "nan")]
    df = df.drop_duplicates(subset=["항목"], keep="last")
    return df.reset_index(drop=True)
```

### 중복 키 정책

첫 번째 열을 키로 쓸 때 같은 항목이 여러 번 나오면 단순 merge가 행을 곱집합으로 늘릴 수 있다. 다음 정책 중 하나를 명시한다.

- `keep="last"`: 같은 페이지에서 마지막 값을 채택한다.
- 값 병합: 중복 값을 `"; "`로 연결한다.
- 복합 키: `페이지 + 항목`, 또는 `표 제목 + 항목`을 키로 사용한다.
- 위치 기반 매칭: 같은 항목명이 반복되는 문서에서는 표 ID와 행 번호를 키에 포함한다.

## 4. 4단계 — 구조적 DataFrame 매칭 및 규칙 기반 요약

### 4.1 판정 규칙

첫 번째 열을 Key, 두 번째 열을 Value로 보고 이전·변경 후 DataFrame을 full outer join한다. Pandas의 `outer`는 양쪽 키의 합집합을 유지하는 full outer join이다. [web:34]

- 양쪽 값이 같음 → 결과에서 제외.
- 양쪽 값이 있고 다름 → `🔄 수정 (Modified)`.
- 신버전에만 있음 → `➕ 추가 (Added)`.
- 구버전에만 있음 → `❌ 삭제 (Deleted)`.

### 4.2 운영용 구현

사용자가 제시한 레시피의 빈 리스트 구문과 `nan` 문자열 의존성을 보완하고, 입력 DataFrame을 직접 변경하지 않도록 복사한다.

```python
import pandas as pd


def _norm_value(value):
    if pd.isna(value):
        return None
    value = str(value).replace("\\n", " ").strip()
    return value if value else None


def generate_pure_diff_table(
    old_df: pd.DataFrame,
    new_df: pd.DataFrame,
    key_col: str | None = None,
    old_value_col: str | None = None,
    new_value_col: str | None = None,
) -> pd.DataFrame:
    """LLM 없이 두 표 DataFrame을 비교해 변경 요약표를 생성한다."""
    old = old_df.iloc[:, :2].copy()
    new = new_df.iloc[:, :2].copy()

    old.columns = [key_col or "항목", old_value_col or "기존_사양"]
    new.columns = [key_col or "항목", new_value_col or "변경_후_사양"]
    key = key_col or "항목"

    for frame in (old, new):
        frame[key] = frame[key].map(clean_cell)
        frame.drop(frame.index[frame[key].eq("")], inplace=True)

    # 중복 키가 있으면 한 키에 여러 값이 대응하도록 명시적으로 집계
    old = old.groupby(key, as_index=False, sort=False).agg(
        {old.columns[1]: lambda s: " ; ".join(dict.fromkeys(x for x in map(clean_cell, s) if x))}
    )
    new = new.groupby(key, as_index=False, sort=False).agg(
        {new.columns[1]: lambda s: " ; ".join(dict.fromkeys(x for x in map(clean_cell, s) if x))}
    )

    merged = pd.merge(old, new, on=key, how="outer", sort=False)
    rows = []
    for _, row in merged.iterrows():
        item = row[key]
        old_val = _norm_value(row.get(old.columns[1]))
        new_val = _norm_value(row.get(new.columns[1]))
        if old_val == new_val:
            continue
        if old_val is None:
            kind = "➕ 추가 (Added)"
            old_display, new_display = "-", new_val or "-"
        elif new_val is None:
            kind = "❌ 삭제 (Deleted)"
            old_display, new_display = old_val, "-"
        else:
            kind = "🔄 수정 (Modified)"
            old_display, new_display = old_val, new_val
        rows.append({
            "변경 항목": item,
            "기존 사양": old_display,
            "변경 후 사양": new_display,
            "변경 구분": kind,
        })

    return pd.DataFrame(rows, columns=[
        "변경 항목", "기존 사양", "변경 후 사양", "변경 구분"
    ])
```

### 4.3 값 비교 개선

문서에 따라 `220 V`와 `220V`, `IC-123`과 `IC 123`을 같은 값으로 볼지 결정해야 한다. 기본값은 원문 보존의 정확성을 위해 문자열을 그대로 비교한다. 필요할 때만 비교용 정규화 함수를 별도로 적용한다.

```python
import re

def comparable(value: str | None) -> str | None:
    if value is None:
        return None
    value = value.casefold()
    value = re.sub(r"\\s+", "", value)
    return value
```

운영 권장 방식은 출력에는 원문을 보존하고, 판정에만 `comparable()`을 사용하며, `판정 기준`과 `원문 값`을 메타데이터에 기록하는 것이다. 단위나 숫자는 무리하게 제거하지 않는다. `1.0 A`와 `1 A`를 동일하게 볼지는 도메인 규칙으로 명시한다.

## 5. 본문 텍스트 diff

본문은 줄 단위 `difflib.ndiff`로 추가·삭제를 구조화할 수 있다. `ndiff`는 `- `가 첫 번째 입력에만 존재, `+ `가 두 번째 입력에만 존재, 공백 코드가 공통 줄을 뜻한다. [web:43]

```python
import difflib
import pandas as pd


def generate_text_diff_table(old_text: str, new_text: str) -> pd.DataFrame:
    old_lines = old_text.splitlines()
    new_lines = new_text.splitlines()
    diff = difflib.ndiff(old_lines, new_lines)
    rows = []
    old_no = new_no = 1

    for entry in diff:
        code, content = entry[:2], entry[2:].strip()
        if code == "?":
            continue
        if code == "-":
            if content:
                rows.append({"위치(라인)": f"이전 문서 {old_no}행", "내용": content, "변경 구분": "삭제"})
            old_no += 1
        elif code == "+":
            if content:
                rows.append({"위치(라인)": f"새 문서 {new_no}행", "내용": content, "변경 구분": "추가"})
            new_no += 1
        else:
            old_no += 1
            new_no += 1
    return pd.DataFrame(rows, columns=["위치(라인)", "내용", "변경 구분"])
```

주의: `ndiff`는 줄의 삽입·삭제를 보여주지만, 복잡한 재배치나 문단 이동을 의미론적으로 이해하지 않는다. 본문이 줄바꿈 방식에 민감하면 문장·문단 단위로 먼저 분할하고, 페이지 번호와 bounding box를 별도로 붙인다.

## 6. Streamlit 연결

`st.dataframe`은 pandas DataFrame을 포함한 여러 dataframe-like 입력을 받아 정렬·검색·복사 가능한 인터랙티브 표로 표시한다. [web:31]

```python
import streamlit as st

st.set_page_config(page_title="PDF 변경사항 비교", layout="wide")
st.title("PDF 사양 변경사항 비교")

old_file = st.file_uploader("구버전 PDF", type=["pdf"], key="old")
new_file = st.file_uploader("신버전 PDF", type=["pdf"], key="new")

if old_file and new_file:
    # 실제 앱에서는 2단계 추출 결과를 사용
    old_df = pd.DataFrame([["정격 전압", "220V"], ["제조사", "ABC"]])
    new_df = pd.DataFrame([["정격 전압", "230V"], ["제조사", "ABC"], ["IC 모델명", "IC-900"]])
    result = generate_pure_diff_table(old_df, new_df)
    st.subheader("표 변경사항")
    st.dataframe(result, width="stretch", hide_index=True)
    st.download_button("CSV 다운로드", result.to_csv(index=False).encode("utf-8-sig"), "diff.csv", "text/csv")
```

실제 구현에서는 업로드 파일을 임시 파일로 저장한 뒤 추출 함수에 전달한다. 페이지·표 단위 결과를 합칠 때 `문서 버전`, `페이지`, `표 번호`, `표 제목`, `추출 파서`, `추출 신뢰도` 컬럼을 추가하면 감사 추적이 쉬워진다.

## 7. 오픈소스 후보군

| 프로젝트 | 역할 | 적합한 경우 | 주의 |
|---|---|---|---|
| [pdfplumber](https://github.com/jsvine/pdfplumber) | PDF 객체·텍스트·표 추출 | 디지털 PDF, 좌표 기반 디버깅 | OCR 표에는 약함; 자체 OCR 아님 [web:1] |
| [pypdf_table_extraction](https://github.com/py-pdf/pypdf_table_extraction) | Camelot 계열 표 추출 | 기존 코드 호환 | 2025-04-11 아카이브·deprecated; 신규는 Camelot 검토 [web:10] |
| [Camelot](https://github.com/camelot-dev/camelot) | lattice/stream 표 추출 | 선 기반·텍스트 기반 표 | PDF 유형과 Ghostscript 등 설치 조건 확인 |
| [Marker](https://github.com/datalab-to/marker) | PDF→Markdown/JSON/HTML, 표 전용 변환 | 복잡한 레이아웃·스캔 보완 | 모델·GPU·가중치 라이선스 확인; `TableConverter` 제공 [web:16] |
| [Table Transformer](https://huggingface.co/docs/transformers/main/model_doc/table-transformer) | 표 검출·구조 인식 | 이미지 표, 구조 인식 | OCR과 셀 텍스트 조합 필요 [web:21] |
| [PDF-Extract-Kit](https://huggingface.co/opendatalab/PDF-Extract-Kit-1.0) | PDF 레이아웃·OCR·표 모델 묶음 | 복합 문서·모델 기반 파이프라인 | 모델별 라이선스·VRAM 확인 [web:19] |
| [GMFT](https://github.com/conjuncts/gmft) | PDF 표→CSV/JSON/DataFrame | 빠른 table extraction | 내부 모델 다운로드와 환경 확인 |

권장 조합은 다음과 같다.

1. 디지털 PDF: `pdfplumber` 또는 Camelot으로 추출 → 정규화 → pure diff.
2. 표 선이 불안정한 PDF: 두 파서의 결과를 비교하고 parsing report/셀 수/키 누락을 품질 신호로 사용.
3. 스캔 PDF: OCR·Table Transformer 또는 Marker로 구조화 → 동일한 정규화·pure diff 적용.
4. 어려운 표만 모델/LLM 보완: 최종 변경 판정은 규칙 기반 결과를 기준으로 유지.

## 8. 품질 검증

### 추출 검증

- 원본 페이지 이미지와 추출 표를 나란히 비교한다.
- 표의 행·열 수, 필수 키 존재 여부, 빈 셀 비율을 검사한다.
- 파서 신뢰도 지표가 낮거나 키가 급감하면 “수정”으로 확정하지 말고 검토 대상으로 표시한다.
- OCR 결과는 숫자 `0/O`, `1/I`, 소수점, 단위, 마이너스 부호를 우선 검수한다.

### 비교 검증

- 키 중복을 먼저 해결한다.
- 값 정규화 규칙을 버전 관리한다.
- 변경 없음 결과도 내부 로그에 남기되 UI에는 변경 행만 표시한다.
- 파서 실패로 한쪽 표가 빈 경우, 모든 행이 삭제/추가로 나오는 것을 막기 위해 예외 처리한다.

```python

def validate_diff_inputs(old_df, new_df, required_keys=None):
    if old_df.empty and new_df.empty:
        raise ValueError("양쪽 표가 모두 비어 있습니다.")
    if required_keys:
        for key in required_keys:
            if key not in set(old_df.iloc[:, 0]) | set(new_df.iloc[:, 0]):
                raise ValueError(f"필수 키가 없습니다: {key}")
```

### 결과 신뢰도 레이블

- `확정`: 두 입력 표가 정상 추출되고 키·값 비교가 완료됨.
- `검토 필요`: OCR, 낮은 parsing accuracy, 중복 키, 표 구조 불일치가 있음.
- `추출 실패`: 표가 없거나 입력 중 하나가 비어 있음.

## 9. 테스트 레시피

```python

def test_diff_cases():
    old = pd.DataFrame([
        ["유지", "A"],
        ["수정", "B"],
        ["삭제", "C"],
    ])
    new = pd.DataFrame([
        ["유지", "A"],
        ["수정", "B2"],
        ["추가", "D"],
    ])
    result = generate_pure_diff_table(old, new)
    assert set(result["변경 구분"]) == {
        "🔄 수정 (Modified)", "➕ 추가 (Added)", "❌ 삭제 (Deleted)"
    }
    assert "유지" not in set(result["변경 항목"])
```

테스트 데이터는 다음 케이스를 포함해야 한다.

- 변경 없음.
- 값 수정.
- 항목 추가·삭제.
- 공백·줄바꿈만 다른 값.
- 중복 키.
- 빈 문자열·NaN.
- 표 헤더 반복.
- 페이지가 달라진 동일 항목.
- OCR 숫자 오인식.
- 한쪽 파서만 표를 추출한 경우.

## 10. 배포 구조

```text
app/
├── app.py                 # Streamlit UI
├── extractors/
│   ├── pdfplumber_ext.py
│   ├── camelot_ext.py
│   └── ocr_table_ext.py
├── normalize.py
├── diff_rules.py          # 4단계 핵심
├── validators.py
├── tests/
├── requirements.txt
└── README.md
```

`requirements.txt` 예시:

```text
pandas
streamlit
pdfplumber
camelot-py
pypdf
```

OCR·모델 경로는 기본 설치와 분리한다. GPU 모델을 필수 의존성으로 넣으면 단순 디지털 PDF 비교까지 무거워지므로 `requirements-ocr.txt` 또는 optional extra로 관리한다.

## 11. 최종 권장안

- 1~3단계는 기존 PDF 업로드·추출·정규화 흐름을 유지한다.
- 4단계의 기본 경로는 `DataFrame → key 정규화 → outer join → Added/Deleted/Modified 분기 → st.dataframe`으로 고정한다.
- LLM은 변경 판정기가 아니라 추출 보완·설명 보조로만 선택적으로 사용한다.
- `pypdf_table_extraction`은 기존 레시피의 참고·호환 대상으로 두고, 신규 운영은 유지보수되는 Camelot 또는 pdfplumber를 우선 검토한다. [web:10]
- 스캔 PDF는 OCR/표 구조 인식 경로를 사용하되, 최종 diff는 동일한 규칙 기반 함수로 통일한다.
- 결과에는 원본 해시, 페이지·표 번호, 파서, 품질 신호, 정규화 규칙, 처리 버전을 남긴다.

이 구성이면 요약문 생성이 아니라 “어떤 키의 값이 어떻게 바뀌었는가”를 데이터와 규칙으로 추적할 수 있으며, Streamlit에서 즉시 검토·다운로드할 수 있다.
