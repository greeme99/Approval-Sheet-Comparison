📑 Full-Version 가이드: Pure Python 기반 승인원 PDF 비교 웹앱 개발📌 0. 아키텍처 개요 (Tech Stack)UI/Frontend: Streamlit (무료, 빠른 웹앱 렌더링)PDF Parsing: pdfplumber (복잡한 사양 테이블 구조적 추출), PyMuPDF (fitz) (초고속 이미지 변환)Diff Engine: Pandas (데이터프레임 Outer Join 연산), difflib (텍스트 행 단위 줄바꿈 비교), OpenCV (cv2) (도면 및 레이아웃 픽셀 단위 시각적 차이점 추출)특징: 유료 LLM이나 외부 RAG 솔루션 없이 100% 로컬 오퍼레이션으로 구동되어, 제조 기업의 핵심 기술 자산인 승인원의 보안 유출 리스크를 완벽히 차단합니다.💻 전체 통합 소스 코드 (app.py)바이브코딩(Vibe Coding) 환경이나 로컬 VS Code 환경에 파일명을 app.py로 생성한 후 아래 코드를 그대로 붙여넣어 실행할 수 있습니다.pythonimport streamlit as st
import pdfplumber
import fitz  # PyMuPDF
import cv2
import numpy as np
import pandas as pd
import difflib
from PIL import Image
import io

# 페이지 레이아웃 설정
st.set_page_config(layout="wide", page_title="승인원 비교 분석기")

# ==========================================
# [핵심 로직] 2단계: PDF 파싱 및 데이터 추출
# ==========================================
def extract_pdf_data(file_bytes):
    """
    pdfplumber와 PyMuPDF를 활용하여 텍스트, 표, 페이지 이미지를 동시에 추출
    """
    text_content = []
    tables_content = []
    
    # 1. 텍스트 및 표 추출 (pdfplumber)
    with pdfplumber.open(io.BytesIO(file_bytes)) as pdf:
        for page in pdf.pages:
            # 텍스트 추출
            page_text = page.extract_text()
            if page_text:
                text_content.append(page_text)
            
            # 표 추출
            extracted_tables = page.extract_tables()
            for table in extracted_tables:
                if table:
                    # 데이터프레임으로 변환 후 저장
                    df = pd.DataFrame(table)
                    tables_content.append(df)
                    
    # 2. 고화질 이미지 렌더링 (PyMuPDF)
    images = []
    doc = fitz.open(stream=file_bytes, filetype="pdf")
    for page in doc:
        pix = page.get_pixmap(matrix=fitz.Matrix(2, 2))  # 2배 확대 (고화질)
        img_data = pix.tobytes("png")
        img = Image.open(io.BytesIO(img_data))
        images.append(img)
        
    full_text = "\n".join(text_content)
    return full_text, tables_content, images

# ==========================================
# [핵심 로직] 3단계: 시각적 변경점 비교 (OpenCV)
# ==========================================
def compare_images(img_old, img_new):
    """
    두 페이지 이미지의 픽셀 차이를 계산하여 변경된 부분을 하이라이트
    """
    # OpenCV 연산을 위해 numpy array (흑백 및 컬러) 변환
    open_cv_old = cv2.cvtColor(np.array(img_old), cv2.COLOR_RGB2BGR)
    open_cv_new = cv2.cvtColor(np.array(img_new), cv2.COLOR_RGB2BGR)
    
    # 크기가 다를 경우 최신 버전에 맞춰 크기 조정 (안정성 확보)
    if open_cv_old.shape != open_cv_new.shape:
        open_cv_old = cv2.resize(open_cv_old, (open_cv_new.shape[1], open_cv_new.shape[0]))
        
    gray_old = cv2.cvtColor(open_cv_old, cv2.COLOR_BGR2GRAY)
    gray_new = cv2.cvtColor(open_cv_new, cv2.COLOR_BGR2GRAY)
    
    # 픽셀 절대값 차이 계산
    diff = cv2.absdiff(gray_old, gray_new)
    _, thresh = cv2.threshold(diff, 30, 255, cv2.THRESH_BINARY)
    
    # 변경된 영역에 바운딩 박스(빨간색 네모) 그리기
    contours, _ = cv2.findContours(thresh, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    highlighted_img = open_cv_new.copy()
    
    for contour in contours:
        if cv2.contourArea(contour) > 50:  # 미세한 노이즈 무시
            x, y, w, h = cv2.boundingRect(contour)
            cv2.rectangle(highlighted_img, (x, y), (x + w, y + h), (0, 0, 255), 2)
            
    # 정적 표출을 위해 다시 PIL 이미지로 변환
    result_img = cv2.cvtColor(highlighted_img, cv2.COLOR_BGR2RGB)
    return Image.fromarray(result_img)

# ==========================================
# [핵심 로직] 4단계: 알고리즘 기반 요약 테이블 생성
# ==========================================
def generate_pure_diff_table(old_tables, new_tables):
    """
    추출된 사양서 표(DataFrame) 데이터를 상호 매칭하여 변경 이력 테이블 연산
    """
    if not old_tables or not new_tables:
        return pd.DataFrame(columns=['변경 항목', '기존 사양', '변경 후 사양', '변경 구분'])
        
    # 예시용 단순화를 위해 양쪽의 첫 번째 주요 스펙 표를 기준으로 병합 연산 수행
    old_df = old_tables[0].copy()
    new_df = new_tables[0].copy()
    
    # 데이터 표준화: 2열 구조(0번: 항목명, 1번: 사양값)로 가정
    if old_df.shape[1] >= 2 and new_df.shape[1] >= 2:
        old_df = old_df.iloc[:, [0, 1]]
        new_df = new_df.iloc[:, [0, 1]]
    else:
        return pd.DataFrame({"알림": ["표 구조가 자동 매칭 기준과 다릅니다. 일반 텍스트 비교를 참조하세요."]})
        
    old_df.columns = ['항목', '기존_사양']
    new_df.columns = ['항목', '변경_후_사양']
    
    old_df['항목'] = old_df['항목'].astype(str).str.strip()
    new_df['항목'] = new_df['항목'].astype(str).str.strip()
    
    # 핵심 알고리즘: '항목' 기준 Outer Join
    merged = pd.merge(old_df, new_df, on='항목', how='outer')
    summary_rows = []
    
    for idx, row in merged.iterrows():
        item = row['항목']
        # 결측값 및 의미 없는 문자 예외 처리
        old_val = str(row['기존_사양']).strip() if pd.notna(row['기존_사양']) and str(row['기존_사양']) != 'None' else None
        new_val = str(row['변경_후_사양']).strip() if pd.notna(row['변경_후_사양']) and str(row['변경_후_사양']) != 'None' else None
        
        if old_val == new_val:
            continue  # 사양 변경 없음
            
        if old_val is None or old_val == 'nan':
            summary_rows.append({'변경 항목': item, '기존 사양': '-', '변경 후 사양': new_val, '변경 구분': '➕ 추가 (Added)'})
        elif new_val is None or new_val == 'nan':
            summary_rows.append({'변경 항목': item, '기존 사양': old_val, '변경 후 사양': '-', '변경 구분': '❌ 삭제 (Deleted)'})
        else:
            summary_rows.append({'변경 항목': item, '기존 사양': old_val, '변경 후 사양': new_val, '변경 구분': '🔄 수정 (Modified)'})
            
    return pd.DataFrame(summary_rows)

def generate_text_diff_table(old_text, new_text):
    """
    일반 승인원 본문 텍스트의 변경 라인을 역추적하여 테이블화
    """
    old_lines = [line.strip() for line in old_text.splitlines() if line.strip()]
    new_lines = [line.strip() for line in new_text.splitlines() if line.strip()]
    
    diff = list(difflib.ndiff(old_lines, new_lines))
    text_diff_rows = []
    
    for line in diff:
        status = line[:2]
        content = line[2:].strip()
        
        if status == '- ':
            text_diff_rows.append({'텍스트 내용': content, '변경 구분': '기존 조항 삭제'})
        elif status == '+ ':
            text_diff_rows.append({'텍스트 내용': content, '변경 구분': '신규 조항 추가'})
            
    return pd.DataFrame(text_diff_rows)


# ==========================================
# [웹 UI 화면 레이아웃] 1단계 및 최종 렌더링
# ==========================================
st.title("📟 전자제품 승인원(PDF) 버전 비교 분석 시스템")
st.markdown("---")

# 파일 업로드 영역
col1, col2 = st.columns(2)
with col1:
    old_file = st.file_uploader("이전 버전 승인원 파일 선택 (이전 회로/스펙)", type=["pdf"])
with col2:
    new_file = st.file_uploader("최신 버전 승인원 파일 선택 (변경 후 사양)", type=["pdf"])

if old_file and new_file:
    with st.spinner("🚀 두 버전의 승인원 데이터를 정밀 비교 연산 중입니다..."):
        # 데이터 파싱 호출
        old_text, old_tables, old_imgs = extract_pdf_data(old_file.read())
        new_text, new_tables, new_imgs = extract_pdf_data(new_file.read())
        
        # 탭 구조 레이아웃 배치
        tab1, tab2 = st.tabs(["📊 정형 사양 변경 요약", "🖼️ 도면/레이아웃 시각적 비교"])
        
        with tab1:
            st.subheader("📝 사양서(스펙 테이블) 변경점 분석 결과")
            # 4단계 대체 로직 작동
            spec_diff_df = generate_pure_diff_table(old_tables, new_tables)
            
            if not spec_diff_df.empty and "알림" not in spec_diff_df.columns:
                st.dataframe(spec_diff_df, use_container_width=True, hide_index=True)
                
                # 통계 대시보드 메트릭 배치
                counts = spec_diff_df['변경 구분'].value_counts()
                st.info(f"💡 통계 요약: 총 {len(spec_diff_df)}건의 변경점 발견 "
                        f"(수정 {counts.get('🔄 수정 (Modified)', 0)}건 | "
                        f"추가 {counts.get('➕ 추가 (Added)', 0)}건 | "
                        f"삭제 {counts.get('❌ 삭제 (Deleted)', 0)}건)")
            else:
                st.success("🎉 감지된 정형 표 사양 변경점이 없습니다.")
                
            st.markdown("---")
            st.subheader("📜 일반 서술형 조항 및 본문 변경 이력")
            text_diff_df = generate_text_diff_table(old_text, new_text)
            if not text_diff_df.empty:
                st.dataframe(text_diff_df, use_container_width=True, hide_index=True)
            else:
                st.success("두 문서의 본문 서술형 텍스트가 일치합니다.")
                
        with tab2:
            st.subheader("🔍 부품 배치 및 회로도 변경 하라이트 (최신 버전 기준)")
            st.caption("※ 빨간색 사각형 영역이 이전 버전 대비 디자인, 도면, 혹은 텍스트 레이아웃이 픽셀 단위로 수정된 구역입니다.")
            
            # 페이지 매칭하여 이미지 루프 렌더링
            min_pages = min(len(old_imgs), len(new_imgs))
            for i in range(min_pages):
                st.markdown(f"#### 📄 Page {i+1} 시각적 비교")
                diff_visual = compare_images(old_imgs[i], new_imgs[i])
                st.image(diff_visual, use_container_width=True)
코드를 사용할 때는 주의가 필요합니다.🚀 로컬 실행 방법 (초보자 가이드)환경 준비: 컴퓨터에 Python이 설치되어 있어야 합니다.필수 오픈소스 라이브러리 설치: 터미널(Terminal) 또는 명령 프롬프트(CMD)를 열고 아래 명령어를 입력합니다.bashpip install streamlit pdfplumber pymupdf opencv-python numpy pandas pillow
코드를 사용할 때는 주의가 필요합니다.애플리케이션 가동: 소스 코드가 저장된 폴더 경로로 이동하여 웹서버를 실행합니다.bashstreamlit run app.py
코드를 사용할 때는 주의가 필요합니다.웹 브라우저 접속: 실행 시 터미널 창에 출력되는 로컬 주소(기본값: http://localhost:8501)로 자동 접속되거나 주소를 복사해 크롬 등 브라우저에 입력하면 앱을 즉시 사용할 수 있습니다.