# 승인원 비교·검수 앱 PRD / 기능 명세서

- 제품명: Revision Lens
- 버전: v1.0
- 추적 기준: `01_SRS_요구사항명세서.md`

## 1. 제품 원칙

1. 자동 판정은 빠르게 찾는 도구이며 최종 승인은 사람이 한다.
2. 변경값마다 원문 페이지와 근거를 보존한다.
3. 출력용 원문과 판정용 정규화 값을 분리한다.
4. 추출 실패를 추가·삭제로 오판하지 않고 검토 필요로 중단한다.

## 2. 사용자 흐름

`기준본·개정본 선택 → 입력 검증 → 구조·사양 추출 → 페이지·필드 비교 → 품질 규칙 실행 → 필터·원문 검토 → 승인/반려/보류 → Excel/PDF 저장`

## 3. 기능 규칙

### F-01 입력과 추출 (`FR-001~004`, `SEC-001~002`)

- 확장자, 파일 크기, PDF 구조, 암호화를 순서대로 검사한다.
- 각 파일의 SHA-256, 페이지 수, 메타데이터를 기록한다.
- `pdfplumber`로 페이지 텍스트·단어를 추출하고, 텍스트 없는 페이지가 있으면 추출 품질을 `검토 필요`로 표시한다.
- 모델번호·등록번호·발행일자 및 로컬 사양 키 사전과 일치하는 행을 `FieldValue`로 변환한다.

### F-02 필드 비교 (`FR-005`)

- 키 합집합을 기준으로 비교한다.
- 구·신 값 모두 존재하고 비교용 정규화 값이 같으면 제외한다.
- 신버전에만 있으면 `추가`, 구버전에만 있으면 `삭제`, 양쪽 값이 다르면 `수정`이다.
- 표시값은 원문을 유지하고 공백·유니코드 정규화는 판정에만 사용한다.
- 같은 키가 반복되면 v1은 마지막 추출값을 대표값으로 사용하고 원문 근거에 페이지를 남긴다.

### F-03 페이지 비교 (`FR-006~007`, `FR-010`)

- v1은 동일 페이지 번호를 우선 대응시킨다.
- 텍스트는 `SequenceMatcher`, 시각 차이는 동일 크기 회색 렌더링의 임계 픽셀 비율로 계산한다.
- 픽셀 차이율 3% 이상은 `시각 변경`, 그 미만이면서 정규화 텍스트가 다르면 `텍스트 변경`, 나머지는 `동일`이다. 텍스트 유사도는 변화 규모를 판단하는 참고값으로 함께 표시한다.
- 한쪽에만 남은 후행 페이지는 추가·삭제로 표시한다.
- 복잡한 페이지 재배열 자동 매칭은 v2 범위이며 v1에서는 사용자가 페이지 근거 화면에서 확인한다.

### F-04 품질 검사 (`FR-008~009`)

| 문제 유형 | 규칙 | 기본 심각도 | 처리 |
|---|---|---|---|
| 필수항목 누락 | 모델번호·등록번호·발행일자 미추출 | 경고 | 해당 위치 입력 안내 |
| 오타 | 로컬 오타 사전 완전 일치 | 경고 | 교정 후보 제시 |
| 표기 불일치 | IIC/I2C 동시 사용, SI 표기 불일치 | 경고/주의 | 표준 용어 확인 안내 |
| 모델·등록번호 불일치 | 표지 모델과 다른 모델 문자열 발견 | 주의 | 인증 기준모델 등 의도 확인 |
| 단위·수치 의심 | 향후 규칙 사전에 등록된 패턴 | 주의 | 설계 원본과 대조 안내 |
| 검토 필요 | 추출 품질 저하·구조 불일치 | 경고 | 확정 판정 금지 |

- 모든 품질 항목은 근거·페이지·신뢰도·수정 가이드를 가진다.
- 모델 참조 불일치는 인증 기준모델일 수 있으므로 자동 오류로 확정하지 않는다.

### F-05 검토와 필터 (`FR-009`, `FR-011`)

- 초기 상태는 `보류`다.
- 사용자는 `승인`, `반려`, `보류` 중 하나를 선택하고 자유 의견을 입력한다.
- 변경 유형, 심각도, 검토 상태 필터는 표시 행 전체에 적용한다.
- 필터로 숨겨진 행의 검토값은 유지한다.

### F-06 보고서 (`FR-012~013`, `NFR-003`)

- Excel: `변경 및 검수`, `페이지 비교`, `처리 정보` 시트를 생성한다.
- PDF: 입력 메타데이터, 검토표, 자동 판정 안내를 포함한다.
- 보류 항목이 있어도 중간 보고서를 내려받을 수 있으나 UI에서 경고한다.
- 보고서에는 앱 버전, 두 파일 해시, 처리 시각, 추출 품질을 기록한다.

## 4. 내부 인터페이스

```python
extract_document(pdf_data: bytes, filename: str) -> DocumentSnapshot
compare_documents(old: DocumentSnapshot, new: DocumentSnapshot) -> ComparisonResult
run_quality_checks(document: DocumentSnapshot) -> list[Issue]
export_excel(result: ComparisonResult, rows: list[dict]) -> bytes
export_pdf(result: ComparisonResult, rows: list[dict]) -> bytes
```

`DocumentSnapshot`은 페이지 텍스트·필드·해시·추출 품질·원본 바이트를, `ComparisonResult`는 변경·문제·페이지 비교를 보유한다.

## 5. 실패와 예외

| 상황 | 사용자 메시지/동작 |
|---|---|
| 한 파일 미선택 | 분석 버튼 비활성화 |
| 비 PDF·빈 파일 | 분석 중단 및 올바른 PDF 재선택 안내 |
| 암호화 PDF | 암호 해제 후 다시 업로드하도록 안내 |
| 손상 PDF | 구조를 읽을 수 없음을 표시하고 결과를 초기화 |
| 텍스트 없는 스캔 PDF | 추출 품질 `검토 필요`; OCR 미지원 안내 |
| 보고서 글꼴 부재 | 설치 안내에 따라 맑은 고딕 또는 지정 폰트 포함 여부 확인 |

## 6. 완료 조건

- `FR-001~014`, `NFR-001~006`, `SEC-001~003`이 테스트 케이스에 연결된다.
- 샘플 기준선 변경과 품질 이슈가 재현된다.
- Windows 실기기 패키징 시험 전에는 Windows 배포를 미검증으로 보고한다.
